#include<stdio.h>
#include<conio.h>
void main()
{
 int a,b;
 printf("Enter two numbers"); //Asking user for input....
 scanf("%d%d",&a,&b);  //Taking input of two variables from users...
 //Here we take a value as 10 and that of b as 20, in this case a should be 20 and b should be 10 at the end...
 a=a+b; //Adding a and b value in variable a , here 1st a will have the sum of the second a and first b, that is a=a(10)+b(20)......
 b=a-b;//here we assign to b the value of a() 
}
